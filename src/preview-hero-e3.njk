---
layout: base.njk
isHome: true
title: "preview: hero E3 — right-set pull-quote"
description: "preview"
scrollTitle: "preview E3 ✦ "
permalink: "preview-hero-e3.html"
---
<style>
    .pv-note {
        background: rgba(0, 255, 204, 0.08);
        border: 1px solid var(--color-border);
        padding: var(--spacing-xs) var(--spacing-md);
        font-family: var(--font-mono);
        font-size: 12px;
        color: var(--color-text-muted);
        text-align: center;
    }
    .pv-note a {
        color: var(--color-accent);
    }
    .pv-d {
        padding: var(--spacing-lg) 0 var(--spacing-lg);
    }
    .pv-d .kicker {
        font-family: var(--font-display);
        font-variation-settings: "MONO" 1, "wght" 550;
        font-size: 11px;
        letter-spacing: 0.22em;
        text-transform: uppercase;
        color: var(--color-accent);
        margin-bottom: var(--spacing-sm);
    }
    .pv-d .var-title {
        font-size: clamp(3.5rem, 19vw, 7.5rem);
        margin: 0 -1px var(--spacing-md);
    }
    .pv-d .screen-rule {
        height: 10px;
        margin-bottom: var(--spacing-lg);
        opacity: 0.8;
        background-size: 7px 7px;
        background-image: radial-gradient(
                circle at 25% 25%,
                var(--color-accent) 0 1px,
                transparent 1.3px
            ),
            radial-gradient(
                circle at 75% 75%,
                var(--color-accent) 0 1px,
                transparent 1.3px
            );
        mask-image: linear-gradient(
            to right,
            transparent,
            black 8%,
            black 92%,
            transparent
        );
        -webkit-mask-image: linear-gradient(
            to right,
            transparent,
            black 8%,
            black 92%,
            transparent
        );
    }

    /* E3 — right-set pull-quote: the bio breaks from the wordmark's left
       edge entirely, set flush-right in Gloock (the plates' "ink
       flourish" serif, otherwise only ever used for colour-match's
       plate) at a large pull-quote size -- a deliberate asymmetry
       against the wordmark's left alignment, rather than everything
       sharing one edge. */
    .pv-e3 .quote {
        text-align: right;
        font-family: var(--font-display-serif);
        font-size: 24px;
        line-height: 1.4;
        max-width: 27ch;
        margin-left: auto;
        color: var(--color-text);
        margin-bottom: var(--spacing-md);
    }
    .pv-e3 .quote-attr {
        text-align: right;
        font-family: var(--font-mono);
        font-size: 11px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        color: var(--color-text-muted);
        margin-bottom: var(--spacing-lg);
    }
    .pv-e3 .inline-links {
        display: flex;
        justify-content: flex-end;
        flex-wrap: wrap;
        gap: var(--spacing-md);
        padding-top: var(--spacing-sm);
        border-top: 1px solid var(--color-border);
    }
    .pv-e3 .inline-links a {
        font-family: var(--font-display);
        font-variation-settings: "MONO" 1, "wght" 500;
        font-size: 13px;
        color: var(--color-text-muted);
    }
    .pv-e3 .inline-links a:hover {
        color: var(--color-accent);
        text-decoration: none;
    }
</style>

<p class="pv-note">
    variation E3 — right-set pull-quote &middot;
    <a href="preview-hero-e1.html">next: E1</a> &middot;
    <a href="preview-hero-e2.html">E2</a> &middot;
    <a href="preview-hero-d.html">D</a> &middot;
    <a href="index.html">back to real site</a>
</p>

<section class="pv-d pv-e3">
    {% include "pv-header.njk" %}
    <p class="quote">
        "A rare combination of art and IT skills" — pre-press
        apprentice, self-hosted everything, Melbourne.
    </p>
    <p class="quote-attr">el &middot; design + dev</p>
    <div class="inline-links">
        <a href="https://www.linkedin.com/in/eloise-bartrop-braybon-407060212/"
            >linkedin</a
        >
        <a href="https://cherryslabs.elphiene.com">cherry's labs</a>
        <a href="mailto:el@rgb-b.com">email</a>
    </div>
</section>
